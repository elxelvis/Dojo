/*import { render, screen } from '@testing-library/react';
import Home from '../Home';

test('renders learn react link', () => {
    render(<Home title=" Blogs" />);
    const homeElement = screen.getByRole(/blogs/i);
    expect(homeElement).toBeInTheDocument();

}) */ 